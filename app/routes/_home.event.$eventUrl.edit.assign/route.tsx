import type { Route } from '.react-router/types/app/routes/_home.event.$eventUrl.edit.assign/+types/route'

export const loader = ({ params }: Route.LoaderArgs) => {
	return {}
}

export default function AssignPlayersPage({
	loaderData
}: Route.ComponentProps) {}
